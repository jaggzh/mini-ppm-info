#include <Arduino.h>
#include "mini-ppm-info.h"
/* This is ONLY a small sketch to make sure the build works;
   the library is in src/
   This test, right now, does not run any actual runtime tests,
   only builds (unless it doesn't).
   An actual test of the functionality is in the tests/
   folder and is designed for testing on a PC, outside of
   the Arduino IDE, for debugging.
   See tests/ folder for an offline (PC) version of that.
 */

void setup() {
}
void loop() {
}
